version: '3.8'

services:
  arangodb:
    image: arangodb:3.11
    container_name: nim-arango-test
    environment:
      ARANGO_ROOT_PASSWORD: password
    ports:
      - "8529:8529"
    volumes:
      - arango-data:/var/lib/arangodb3

volumes:
  arango-data:
